# TEST.md — path canonicalization fixture

This file is a deliberate test fixture used by NM's path canonicalization
(`nm_db.canonical_path`) and the matching MCP tooling. Don't rename, move,
or delete it without updating [`SCHEMA.md`](SCHEMA.md) and the smoke tests
that reference it.

The body below is intentionally trivial — only the path matters.

---

Hello world
